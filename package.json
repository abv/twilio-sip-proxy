{
  "name": "twilio-sip-proxy",
  "version": "0.0.2",
  "description": "Run a SIP proxy to a Twilio SIP endpoint",
  "repository" :
    { "type" : "git"
    , "url" : "https://github.com/abv/twilio-sip-proxy.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adam Varga <me@adamvarga.com>",
  "license": "ISC",
  "dependencies" : {
      "sip" : "0.0.2-42"
  }
}
